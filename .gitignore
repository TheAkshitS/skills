.DS_Store
node_modules/
*.swp
*.swo
*~
.env
.env.*
.claude/settings.local.json

temp
.superpowers/

# skills.sh consumer-side install lockfile; created by `npx skills add`, not a
# source artifact of this repo (this repo is the skills' source, not a consumer).
skills-lock.json
